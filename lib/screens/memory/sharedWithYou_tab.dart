import 'package:flutter/material.dart';

class SharedWithYouTab extends StatelessWidget {
  const SharedWithYouTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Shared with You Content',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
